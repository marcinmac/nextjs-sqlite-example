/** @type {import('next').NextConfig} */
 
const nextConfig  = {
    // distDir: 'build', // Custom build directory
    output: 'standalone',
    trailingSlash: true,
  };



export default nextConfig;

